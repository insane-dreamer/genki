#headerWrap
  = render :partial => "layouts/search"
  = render :partial => "layouts/submit"
  #headerTop
    #flash= show_flash() if ( flash[:notice] || flash[:error] )
    = search_and_submit_links
    = link_to "ABOUT", about_path
    = link_to "NOTICES", section_path('notices') if Section.find_by_name('notices')
    = link_to "RESOURCES", wtw_resources_path
    = image_tag('loading.gif', :id => 'spinner', :style => 'display:none;')
