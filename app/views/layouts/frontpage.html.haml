= content_for(:page_title) do 
  = config[:title]
= content_for(:head) do 
  = auto_discovery_link_tag(:atom, rss_path(:tag => @tag, :format => 'atom'))
= content_for(:content) do  
  #feature
    #featureLeft
      = frontpage_tabs(@section || 'TWEET')
    #featureRight
      #frontpage_posts_wrapper
        #frontpage_posts
          .scroller
            #scrollcontent
              = yield(:frontpage_content)
            
  #arrows
    = yield(:navigation)

= render :file => 'layouts/application'