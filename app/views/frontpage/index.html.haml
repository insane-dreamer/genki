= content_for(:page_title) do posts_title(@tag) end 
= content_for(:head) do auto_discovery_link_tag(:atom, rss_path(:tag => @tag, :format => 'atom')) end

#feature
  #featureLeft
    = frontpage_tabs(@section)
  #featureRight
    #frontpage_posts_wrapper
      #frontpage_posts
        .scroller
          #scrollcontent
            = render :partial => "posts", :locals => {:posts => @posts}

#arrows= frontpage_navigation(@page,@section)
